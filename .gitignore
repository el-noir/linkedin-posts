# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/
build/

# production
dist/

# env files (NEVER commit secrets)
.env
.env.local
.env*.local

# debugging
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# os
.DS_Store
Thumbs.db

# editor
.vscode/
.idea/
*.swp
*.swo

# test artifacts
coverage/
*.lcov

# generated posts (runtime, user-owned)
posts/
uploads/
tmp/

# fonts (bundled at build, not source)
fonts/

# LiteLLM proxy logs + config (local testing only — contains no secrets,
# but kept local so different machines can point at different models)
litellm_config.yaml
litellm_*.log

# dev server logs (local only)
dev_*.log

# generated PDFs (runtime output)
*.pdf

# typescript incremental build info
*.tsbuildinfo
